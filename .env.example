# Server
PORT=5000
NODE_ENV=development

# MongoDB (required)
MONGODB_URI=mongodb+srv://mujeeb:SP1K0VFloyhMnP81@cluster0.ua4mdos.mongodb.net/?appName=Cluster0

# JWT (required for auth)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=your-refresh-secret-key
JWT_REFRESH_EXPIRE=30d

# Frontend URL (for CORS) - use production URL when deployed
CLIENT_URL=http://localhost:5173

# Optional: AdSense client ID for ad revenue (frontend uses VITE_ADSENSE_CLIENT_ID)
