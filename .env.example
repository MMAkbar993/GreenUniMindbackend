# MongoDB (required for Atlas)
MONGODB_URI=mongodb://localhost:27017/greenunimind

# JWT (required for auth)
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRE=7d

# Optional
PORT=5001

# Frontend URL (for CORS and redirects). No trailing slash.
FRONTEND_URL=http://localhost:8080

# Production: comma-separated origins for CORS (e.g. Vercel frontend). No trailing slashes.
# CORS_ORIGINS=https://green-uni-mindforntend.vercel.app

# Email (verification codes). If not set, code is logged to console only.
# --- Gmail: use App Password (not your normal password) ---
# 1. Google Account → Security → 2-Step Verification ON
# 2. Security → App passwords → Generate for "Mail"
# 3. Use the 16-character password below as SMTP_PASS
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-gmail@gmail.com
SMTP_PASS=your-16-char-app-password
MAIL_FROM=your-gmail@gmail.com
APP_NAME=GreenUniMind
